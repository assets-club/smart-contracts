import { join } from 'path';

export const data = join(__dirname, '../../data');
export const OG_FILE = join(data, 'og.csv');
export const ACCESS_LIST_FILE = join(data, 'access_list.csv');
export const CLAIMS_FILE = join(data, 'claims.csv');
